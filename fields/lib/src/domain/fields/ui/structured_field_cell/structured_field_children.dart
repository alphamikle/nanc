import 'package:fields/src/domain/fields/logic/dynamic_field/dynamic_field_item.dart';
import 'package:flutter/material.dart';

class StructuredFieldChildren extends StatelessWidget {
  const StructuredFieldChildren({
    required this.items,
    super.key,
  });

  final List<DynamicFieldItem> items;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
