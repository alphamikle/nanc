name: svg_renderer
description: SVG Renderer Package for the Nanc
publish_to: none
repository: https://github.com/alphamikle/nanc
homepage: https://nanc.io
version: 0.0.1

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=3.10.5"

dependencies:
  flutter:
    sdk: flutter
  nanc_renderer:
    path: ../nanc_renderer
  tools:
    path: ../tools
  icons:
    path: ../icons
  flutter_svg: ^2.0.7
  vector_graphics: ^1.1.7
  json_annotation: ^4.8.1
  copy_with_extension: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint:
    path: ../lint
  flutter_lints: ^2.0.1
  json_serializable: ^6.7.0
  copy_with_extension_gen: ^5.0.2
  build_runner: ^2.4.4

flutter: