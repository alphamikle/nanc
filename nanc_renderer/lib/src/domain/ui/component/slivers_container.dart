import 'package:flutter/material.dart';

class SliversContainer {
  const SliversContainer({
    required this.widgets,
    required this.isSlivers,
  });

  final List<Widget> widgets;
  final bool isSlivers;
}
