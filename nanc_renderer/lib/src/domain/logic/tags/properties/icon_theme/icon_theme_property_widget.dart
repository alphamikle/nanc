import 'package:flutter/material.dart';

import '../../property_widget.dart';

class IconThemePropertyWidget extends PropertyWidget<IconThemeData> {
  const IconThemePropertyWidget({
    required super.name,
    required super.property,
    super.key,
  });
}
