name: nanc_api_supabase
description: Supabase API Implementation for Nanc
version: 1.0.0
homepage: nope
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  nanc_config:
    path: ../nanc_config
  model:
    path: ../model
  tools:
    path: ../tools
  fields:
    path: ../fields
  supabase_flutter: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  lint:
    path: ../lint