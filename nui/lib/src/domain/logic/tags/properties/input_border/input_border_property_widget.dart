import 'package:flutter/material.dart';

import '../../property_widget.dart';

class InputBorderPropertyWidget extends PropertyWidget<InputBorder> {
  const InputBorderPropertyWidget({
    required super.name,
    required super.property,
    super.key,
  });
}
