import 'package:flutter/material.dart';

import '../../property_widget.dart';

class InputDecorationPropertyWidget extends PropertyWidget<InputDecoration> {
  const InputDecorationPropertyWidget({
    required super.name,
    required super.property,
    super.key,
  });
}
