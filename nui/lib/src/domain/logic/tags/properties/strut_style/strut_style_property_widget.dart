import 'package:flutter/material.dart';

import '../../property_widget.dart';

class StrutStylePropertyWidget extends PropertyWidget<StrutStyle> {
  const StrutStylePropertyWidget({
    required super.name,
    required super.property,
    super.key,
  });
}
