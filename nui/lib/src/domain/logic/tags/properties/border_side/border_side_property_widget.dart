import 'package:flutter/material.dart';

import '../../property_widget.dart';

class BorderSidePropertyWidget extends PropertyWidget<BorderSide> {
  const BorderSidePropertyWidget({
    required super.name,
    required super.property,
    super.key,
  });
}
