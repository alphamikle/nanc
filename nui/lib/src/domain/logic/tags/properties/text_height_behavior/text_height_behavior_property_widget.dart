import 'package:flutter/material.dart';

import '../../property_widget.dart';

class TextHeightBehaviorPropertyWidget extends PropertyWidget<TextHeightBehavior> {
  const TextHeightBehaviorPropertyWidget({
    required super.name,
    required super.property,
    super.key,
  });
}
